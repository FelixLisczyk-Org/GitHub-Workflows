name: Xcode Update Swift Package Dependencies
description: Xcode Update Swift Package Dependencies
inputs:
  github_token:
    description: "GitHub Access Token"
  platform:
    description: "Target Build Platform"
  scheme:
    description: "Xcode Scheme Name"
  retry_failed_tests:
    description: "Retry Failed Tests"
    required: false
    default: false
  skip_tests:
    description: "Skip Running Tests"
    required: false
    default: false
  ios_simulator_name:
    description: "Preferred iOS simulator device name"
    required: false
    default: "iPhone 17 Pro"

runs:
  using: "composite"
  steps:
    - name: Update Host App Dependencies
      run: |
        if [ -f Tuist.swift ]; then
          TUIST_USE_SWIFTERPM=0 tuist install --update && tuist install
        fi
      shell: bash -leo pipefail {0}
    - name: Update Swift Package Dependencies
      run: swift package update
      shell: bash -leo pipefail {0}
    - name: Generate Xcode Project
      run: |
        if [ -f Tuist.swift ]; then
          tuist install && tuist generate --no-open
        fi
      shell: bash -leo pipefail {0}
    - name: Run Unit Tests
      uses: FelixLisczyk-Org/GitHub-Workflows/xcode-test-package@main
      with:
        platform: ${{ inputs.platform }}
        scheme: ${{ inputs.scheme }}
        retry_failed_tests: ${{ inputs.retry_failed_tests }}
        ios_simulator_name: ${{ inputs.ios_simulator_name }}
      if: ${{ inputs.skip_tests != 'true' }}
    - name: Commit & Push changes
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add -A
        git diff --staged --quiet || git commit -m "Updated dependencies [skip ci]"
        git push
      shell: bash -leo pipefail {0}
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
